apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "garage.fullname" . }}-web
  labels:
    {{- include "garage.labels" . | nindent 4 }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    external-dns.alpha.kubernetes.io/ttl: "300"
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
        - {{ .Values.domain.web | quote }}
      secretName: {{ include "garage.fullname" . }}-web-tls
  rules:
    - host: {{ .Values.domain.web | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "garage.fullname" . }}
                port:
                  number: 3902
